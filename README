This is Perl module Acme::Crypt::LLDecade.

INSTALLATION

Acme::Crypt::LLDecade installation is straightforward. If your CPAN shell is set up,
you should just be able to do

    % cpan Acme::Crypt::LLDecade
or

    % cpanm Acme::Crypt::LLDecade

Download it, unpack it, then build it as per the usual:

    % perl Makefile.PL
    % make && make test

Then install it:

    % make install

DOCUMENTATION

Acme::Crypt::LLDecade documentation is available as in POD. So you can do:

    % perldoc Acme::Crypt::LLDecade

to read the documentation online with your favorite pager.

xaicron
